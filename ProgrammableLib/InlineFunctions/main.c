#include "a.h"
#include "b.h"

int main()
{
    //Comment
    int a = square(15);
    int result = min(5,10);

    int finalResult = -1;    
    finalResult = sum(10,15);
    finalResult = multiply(10,15);
}